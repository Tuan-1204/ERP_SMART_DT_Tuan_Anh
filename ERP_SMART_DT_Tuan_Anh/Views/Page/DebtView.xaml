<UserControl x:Class="ERP_SMART_DT_Tuan_Anh.Views.Page.DebtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="16" Background="#F8FAFC">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- KHU VỰC THAO TÁC LẬP CHỨNG TỪ (BÊN TRÁI) -->
        <Border Style="{StaticResource CardStyle}" Padding="18" Margin="0,0,12,0" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#E2E8F0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Quản lý thanh toán công nợ" FontSize="22" FontWeight="Bold" Margin="0,0,0,4" Foreground="#1A202C"/>
                    <TextBlock Text="Lập phiếu thu/chi tất toán số dư nợ đối tác" FontSize="13" Foreground="#718096" Margin="0,0,0,16"/>

                    <!-- CHỌN ĐỐI TÁC -->
                    <TextBlock Text="Đối tác giao dịch (Khách buôn / Nhà cung cấp)" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568"/>
                    <ComboBox ItemsSource="{Binding Partners}"
                              SelectedItem="{Binding SelectedPartner}"
                              DisplayMemberPath="FullName"
                              Height="42" Margin="0,6,0,14" VerticalContentAlignment="Center"/>

                    <!-- KHỐI MINH BẠCH THÔNG TIN NỢ (TỐI ƯU UI/UX CAO CẤP) -->
                    <Border Background="#F7FAFC" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,14">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Text="Phân loại đối tác:" FontSize="13" Foreground="#4A5568"/>
                                <TextBlock Text="{Binding PartnerTypeText}" HorizontalAlignment="Right" FontSize="13" FontWeight="SemiBold" Foreground="#2B6CB0"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Dư nợ hiện tại:" FontSize="13" Foreground="#4A5568"/>
                                <TextBlock Text="{Binding CurrentDebtText}" HorizontalAlignment="Right" FontSize="14" FontWeight="Bold" Foreground="#E53E3E"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- SỐ TIỀN THANH TOÁN (ĐÃ FIX DELAY CHỐNG NHẢY CON TRỎ CHUỘT) -->
                    <TextBlock Text="Số tiền ghi nhận thanh toán (VND)" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568"/>
                    <Grid Margin="0,6,0,14">
                        <TextBox x:Name="TxtAmount" 
                                 Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged, Delay=300}" 
                                 Height="40" VerticalContentAlignment="Center" Padding="8,0,0,0" FontWeight="Bold" FontSize="14"/>
                        <!-- Watermark chữ 0 mờ -->
                        <TextBlock Text="0" Foreground="#A0AEC0" FontSize="14" IsHitTestVisible="False" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=TxtAmount, Path=Text}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- HIỂN THỊ DƯ NỢ SAU KHI GÕ TIỀN -->
                    <Grid Margin="4,0,4,14">
                        <TextBlock Text="Dư nợ sau khi giảm:" FontSize="13" Foreground="#4A5568"/>
                        <TextBlock Text="{Binding RemainingDebtText}" HorizontalAlignment="Right" FontSize="13" FontWeight="SemiBold" Foreground="#319795"/>
                    </Grid>

                    <!-- GHI CHÚ -->
                    <TextBlock Text="Nội dung/Ghi chú chứng từ" FontSize="13" FontWeight="SemiBold" Foreground="#4A5568"/>
                    <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" Height="75" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,6,0,18" Padding="6" VerticalContentAlignment="Top"/>

                    <Button Content="⚡ Xác nhận ghi sổ công nợ"
                            Command="{Binding PayDebtCommand}"
                            Background="#2B6CB0" Foreground="White" FontWeight="Bold" FontSize="15" Height="46" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- BẢNG LỊCH SỬ BIẾN ĐỘNG NỢ RIÊNG BIỆT (BÊN PHẢI) -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="14" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#E2E8F0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Lịch sử biến động công nợ của đối tác được chọn" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" Foreground="#2D3748"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Transactions}" 
                          Style="{StaticResource AppDataGridStyle}" 
                          AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Thời gian ghi sổ" Binding="{Binding TransactionDate, StringFormat=dd/MM/yyyy HH:mm}" Width="145"/>
                        <DataGridTextColumn Header="Mã chứng từ/Hóa đơn" Binding="{Binding BillId}" Width="150" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Loại giao dịch" Binding="{Binding TransactionTypeText}" Width="110"/>
                        <DataGridTextColumn Header="Số tiền biến động" Binding="{Binding Amount, StringFormat={}{0:N0} đ}" Width="140" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Nội dung chi tiết" Binding="{Binding Note}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>