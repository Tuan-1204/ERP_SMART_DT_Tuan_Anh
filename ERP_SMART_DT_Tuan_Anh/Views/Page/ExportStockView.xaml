<UserControl x:Class="ERP_SMART_DT_Tuan_Anh.Views.Page.ExportStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Phiếu xuất kho / bán hàng"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="0,0,0,20"/>

                <TextBlock Text="Khách hàng" FontSize="15" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Customers}"
                          SelectedItem="{Binding SelectedCustomer}"
                          DisplayMemberPath="FullName"
                          Height="42"
                          FontSize="15"
                          Margin="0,6,0,14"/>

                <TextBlock Text="Sản phẩm" FontSize="15" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="ProductName"
                          Height="42"
                          FontSize="15"
                          Margin="0,6,0,14"/>

                <TextBlock Text="Đơn giá bán" FontSize="15" FontWeight="SemiBold"/>
                <TextBox Text="{Binding UnitPrice}" Style="{StaticResource AppTextBoxStyle}" Margin="0,6,0,14"/>

                <TextBlock Text="Khách đã thanh toán" FontSize="15" FontWeight="SemiBold"/>
                <TextBox Text="{Binding PaidAmount}" Style="{StaticResource AppTextBoxStyle}" Margin="0,6,0,14"/>

                <TextBlock Text="Ghi chú"
                           FontSize="15"
                           FontWeight="SemiBold"/>
                <TextBlock Text="Hệ thống chỉ cho phép xuất các IMEI đang ở trạng thái Trong kho. SQL Stored Procedure và Trigger sẽ kiểm tra lần cuối trước khi lưu."
                           TextWrapping="Wrap"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="26,0,0,0">
                <TextBlock Text="Danh sách IMEI xuất kho"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,12"/>

                <TextBox Text="{Binding ImeiText, UpdateSourceTrigger=PropertyChanged}"
                         Height="360"
                         FontSize="15"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>

                <Button Content="Lưu phiếu xuất"
                        Command="{Binding ExportCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="50"
                        Margin="0,20,0,0"/>

                <Button Content="Tải lại dữ liệu"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource OutlineButtonStyle}"
                        Height="44"
                        Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
