<UserControl x:Class="ERP_SMART_DT_Tuan_Anh.Views.Page.ImportStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Phiếu nhập kho"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="0,0,0,20"/>

                <TextBlock Text="Nhà cung cấp"/>
                <ComboBox ItemsSource="{Binding Suppliers}"
                          SelectedItem="{Binding SelectedSupplier}"
                          DisplayMemberPath="FullName"
                          Height="42"
                          Margin="0,6,0,14"/>

                <TextBlock Text="Sản phẩm"/>
                <ComboBox ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="ProductName"
                          Height="42"
                          Margin="0,6,0,14"/>

                <TextBlock Text="Đơn giá nhập"/>
                <TextBox Text="{Binding UnitPrice}" Style="{StaticResource AppTextBoxStyle}" Margin="0,6,0,14"/>

                <TextBlock Text="Đã thanh toán"/>
                <TextBox Text="{Binding PaidAmount}" Style="{StaticResource AppTextBoxStyle}" Margin="0,6,0,14"/>

                <TextBlock Text="Ghi chú"/>
                <TextBox Text="{Binding Note}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="24,0,0,0">
                <TextBlock Text="Danh sách IMEI"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,12"/>

                <TextBox Text="{Binding ImeiText}"
                         Height="360"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>

                <Button Content="Lưu phiếu nhập"
                        Command="{Binding ImportCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="50"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>